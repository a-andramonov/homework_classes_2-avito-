# Classes 2 Homework

Этот проект реализует класс `Advert` для обработки объявлений в формате JSON. Код демонстрирует работу с динамическими атрибутами, свойствами (`property`) и миксинами (`Mixin`).

## 📋 Функционал

1.  **Динамические атрибуты JSON**
    *   Преобразование словаря (JSON) в объект Python.
    *   Обращение к полям через точку: `ad.location.address` вместо `ad['location']['address']`.
    *   Обработка вложенных словарей любой глубины.
    *   Автоматическое переименование конфликтующих ключевых слов (например, `class` -> `class_`).

2.  **Валидация цены (`price`)**
    *   Проверка на отрицательное значение (выбрасывает `ValueError`).
    *   Если цена отсутствует в JSON, по умолчанию устанавливается `0`.

3.  **Цветной вывод (Mixin)**
    *   Использование `ColorizeMixin` для окрашивания строкового представления объекта в консоли.
    *   Цвет текста задается атрибутом класса.

## 🚀 Как запустить

### 1. Клонируйте репозиторий
```bash
git clone <ссылка-на-твой-репозиторий>
cd <папка-проекта>
